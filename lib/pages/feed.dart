import 'package:flutter/material.dart';


class FeedPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // appBar: AppBar(
      //   title: Text('Feed Page'),
      // ),
      body: Center(
        child: Container(
          child: Text("That's your feed"),
        ),
      ),
    );
  }
}